# U4 — Wireframes (recovery-copy)

**Cite:** tip `4e1e6bd` · U0 recovery gate in [`12-U0-WIREFRAMES.md`](12-U0-WIREFRAMES.md) §4 · issue #15

## Recovery (More)

```
Recovery
A recovery key restores access to your folders if this device is lost.
⚠ Loss of your recovery key and this device can mean TOTAL LOSS of access.

<threat copy: friendnet you trust; same invite offers disk; no storage proofs;
 share over I2P; lose the key and this device and access can be gone>

☐ I understand: this is a friendnet I trust, not a company backup. …

[ Restore to Folders ]←primary (disabled until the box is checked)
[ Export recovery key… ]           (disabled until the box is checked)

Last export: never
```

## Export (unchanged dual ACK)

Write stays disabled until both boxes are checked and the passphrase matches (empty passphrase is allowed and labeled plaintext). One click does not write the file.

## After restore

The window is **Folders**. The list names the restored folders. Offer pie stays on that page.
