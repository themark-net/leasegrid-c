# U4 — Non-goals

**Cite:** tip `4e1e6bd` · issue #15

| Not in U4 | Why |
|-----------|-----|
| Tahoe WUI as the product | Forbidden. Recovery stays in the native window and the CLI. |
| Mainnet XMR claims | Payment stays behind `LEASEGRID_GATED` / More → Credit. No new XMR lecture on Folders or Recovery. |
| Replacing the P1 shell | Folders-first, offer disk pie, unpaid default stay. |
| AppImage operate (#12) | Deferred. CLI export/restore is the tip install path and FAILs closed without ACKs. |
| U5 polish | Not this slice. |
| One-click recovery dump | Export needs threat ACK plus both export ACKs. |
| Per-folder path picker (U0 wireframe 4f) | Still not built. Restore lands under `~/Leasegrid/<name>` (`LEASEGRID_RESTORE_ROOT` overrides). |
